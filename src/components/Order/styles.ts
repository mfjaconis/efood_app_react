import styled from "styled-components";
import { colors } from "./../../styles";

export const ContianerOrder = styled.div`
    width: 345px;
    height: 347px;
    color: ${colors.secundary_bg_color};

    h3{
        margin-bottom: 16px;
        font-size: 16px;
        font-weight: 700;
    }

    p{
        font-size: 14px;
        font-weight: 400;
        line-height: 22px; 
    }
`;